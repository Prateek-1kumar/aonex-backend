# @aonex/ingestion-csv-parser

**HLD §11.2 · Phase 3**

Parses templated CSV uploads, validates rows, writes row-level source_artifacts.

This package is a deliberate Phase 3 stub. Folder shipped now so
`ls` reveals the full HLD architecture; implementation lands when
the phase opens. See `docs/HLD-alignment.md`.
