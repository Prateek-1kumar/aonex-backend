# @aonex/ingestion-connector-fetcher

**HLD §11.1 · Phase 1**

Fetches records via Connector Gateway and writes source_artifacts. Phase 1 — implemented inside apps/worker drain.processor.ts.

This package is a deliberate Phase 1 stub. Folder shipped now so
`ls` reveals the full HLD architecture; implementation lands when
the phase opens. See `docs/HLD-alignment.md`.
