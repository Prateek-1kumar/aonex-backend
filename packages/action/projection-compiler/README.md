# @aonex/action-projection-compiler

**HLD §16 · Phase 5**

Pure function: same product_version + adapter_version + overrides → same payload_hash (HLD §16.1).

This package is a deliberate Phase 5 stub. Folder shipped now so
`ls` reveals the full HLD architecture; implementation lands when
the phase opens. See `docs/HLD-alignment.md`.
